fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic[email]>=2.7.0
pydantic-settings>=2.2.0

# Supabase
supabase>=2.4.0

# Google APIs (Calendar + Gmail OAuth)
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.126.0

# Gemini
google-generativeai>=0.5.0

# Telegram
python-telegram-bot>=21.2

# Email inbound parsing
mailgun2>=1.0.0

# HTTP client
httpx>=0.27.0

# Background jobs (if not using pg_cron)
apscheduler>=3.10.4

# Utilities
python-multipart>=0.0.9      # file uploads (photos, PDFs)
python-jose[cryptography]>=3.3.0
python-dotenv>=1.0.0

# PDF parsing (syllabus import)
pypdf>=4.2.0

# Schedule PNG renderer (Playwright is installed separately — see README)
# playwright>=1.44.0         # uncomment after: playwright install chromium

# Error monitoring (optional)
sentry-sdk[fastapi]>=2.3.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                # async test client for FastAPI
structlog>=24.0.0
apscheduler>=3.10.0
